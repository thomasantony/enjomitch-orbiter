# =========================================================================
#     This makefile was generated by
#     Bakefile 0.2.3 (http://www.bakefile.org)
#     Do not modify, all changes will be overwritten!
# =========================================================================



# -------------------------------------------------------------------------
# These are configurable options:
# -------------------------------------------------------------------------
# C compiler 
CC := gcc

# C++ compiler 
CXX := g++

# Standard flags for C++ 
CXXFLAGS := -Wall

# Standard preprocessor flags (common for CC and CXX) 
CPPFLAGS := 

# Standard linker flags 
LDFLAGS := -L/usr/local/lib 

# Location and arguments of wx-config script 
WX_CONFIG := wx-config

# C++ flags to use with wxWidgets code 
WX_CXXFLAGS := `$(WX_CONFIG) --cxxflags`



PROGRAM = client

WINDRES = windres.exe

# MODIFY ACCORDINGLY:
WINDRES_FLAGS = -Ic:/wxWidgets-2.8.9/include/ --input-format=rc -O coff 

# -------------------------------------------------------------------------
# Do not modify the rest of this file!
# -------------------------------------------------------------------------

### Variables: ###

CPPDEPS = -MT$@ -MF`echo $@ | sed -e 's,\.o$$,.d,'` -MD
LISTCTRL_CXXFLAGS =  -I.  $(WX_CXXFLAGS) $(CPPFLAGS) $(CXXFLAGS)
LISTCTRL_OBJECTS = main.o

### Conditionally set variables: ###



### Targets: ###

all: $(PROGRAM)

install: all

uninstall: 

clean: 
	rm -f ./*.o
	rm -f ./*.d
	rm -f $(PROGRAM).exe
	rm -f ./*~
	rm -f ./*.res

$(PROGRAM): $(LISTCTRL_OBJECTS)
	$(CXX) -o $@ $(LISTCTRL_OBJECTS) $(LDFLAGS)  `$(WX_CONFIG) --libs base,net` -lwxcode_mswud_wxsqlite3-2.8 -lsqlite3

main.o: main.cpp
	$(CXX) -c -o $@ $(LISTCTRL_CXXFLAGS) $(CPPDEPS) $<
  
.PHONY: all install uninstall clean

# Dependencies tracking:
#-include ./*.d
